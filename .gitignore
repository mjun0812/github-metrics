# Upstream reference (per constitution: must not enter git history)
org_repo/

# Go build artifacts
/bin/
*.test
*.out
coverage.out

# Asset generation toolchain (M3: @primer/octicons drives gen-octicons,
# but the resolved tree must not enter git history — only the generated
# assets/octicons/data.json is committed).
node_modules/

# Editor / OS
.DS_Store
*.swp
