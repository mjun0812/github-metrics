query UserFollowers($login: String!, $first: Int!, $size: Int!) {
  user(login: $login) {
    followers(first: $first) {
      totalCount
      nodes {
        login
        name
        avatarUrl(size: $size)
      }
    }
    following(first: $first) {
      totalCount
      nodes {
        login
        name
        avatarUrl(size: $size)
      }
    }
  }
}
